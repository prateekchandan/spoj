#include <bits/stdc++.h> 

using namespace std;

struct MyList
{
	vector<int> v;
	int st,end,size;

	MyList(int n)
	{
		v = vector<int>(n);
		size = n;
	}
};

int main(int argc, char const *argv[])
{
	/* code */
	return 0;
}